\subsection{Kernel Suite}
\label{sec:meth:kernels}

% We collect a kernel suite spanning five operator categories
% that cover the dominant computational patterns in modern deep learning.
% In total, our suite comprises 22 kernels:
% 3 \textbf{GEMM} (dense matmul, batched matmul, fused linear+activation),
% 1 \textbf{Attention} (scaled dot-product / FlashAttention variants),
% 1 \textbf{Convolution} (Conv2d, $1\times1$--$7\times7$ filters, depthwise, strided),
% 2 \textbf{Normalization} (LayerNorm, RMSNorm),
% and 15 \textbf{element-wise and reduction} operators
% (add, mul, relu, leaky\_relu, softmax, log\_softmax, logsumexp, swiglu,
%  argmax, max reduction, mean reduction, cross-entropy, matrix transpose, index select, embedding).

% Our 22 kernels are sourced from TritonBench~\cite{li2025tritonbench},
% the TileLang example repository~\cite{wang2025tilelang},
% and custom implementations for configurations absent from both suites.
% We exclude kernels with sparse inputs or runtime-dependent output shapes,
% as these lack meaningful library baselines.

Our benchmark suite covers five operator categories that capture the dominant computation patterns in modern deep learning: matrix multiplication, attention, convolution, normalization, and element-wise or reduction operators. In total, the suite contains 22 kernels: three \textbf{GEMM} workloads (i.e., dense matrix multiplication, batched matrix multiplication, and fused linear plus activation), one \textbf{attention} workload (i.e., scaled dot-product attention / FlashAttention variants), one \textbf{convolution} workload (i.e., Conv2d with $1\times1$ to $7\times7$ filters, including depthwise and strided cases), two \textbf{normalization} workloads (i.e., LayerNorm and RMSNorm), and fifteen \textbf{element-wise or reduction} workloads including add, mul, relu, leaky\_relu, softmax, log\_softmax, logsumexp, swiglu, argmax, max reduction, mean reduction, cross-entropy, matrix transpose, index select, and embedding.

We derive these workloads from TritonBench~\cite{li2025tritonbench}, the TileLang example repository~\cite{wang2025tilelang}, and our own implementations for configurations not covered by either source. We exclude operators with sparse inputs or runtime-dependent output shapes, since they do not admit stable or meaningful library baselines.

% REVISION-NOTE id=4134A-W9 + 4134B-Q1 RESOLVED: selection criteria + coarse provenance added below.
% TODO(revision): add a full per-kernel provenance table (tab:provenance) once each kernel's
% source is verified against ../ViperBench/ dirs --- reviewers A and B requested per-kernel provenance.
\paragraph{Selection criteria.}
We narrow the kernels in TritonBench~\cite{li2025tritonbench} to our 22-kernel suite using four criteria.
First, \emph{forward-pass only}: we exclude backward and gradient kernels, whose performance is governed by different access patterns and reduction structures.
Second, \emph{stable library baseline}: each kernel must admit a well-defined cuBLAS, cuDNN, or PyTorch reference that computes the same function, so that library efficiency is a meaningful quantity.
Third, \emph{five-category coverage}: we retain kernels that populate the five operator categories above rather than over-sampling any single class.
Fourth, \emph{canonical operators}: we exclude operators with sparse inputs or runtime-dependent output shapes, which lack a stable library baseline.
The Triton implementations are drawn from TritonBench, except \texttt{layer\_norm}, which follows the TorchInductor reference; the TileLang implementations are our re-implementations of the same operators, and any kernel absent from both source suites is implemented by us.


\subsection{Baseline Construction}
\label{sec:meth:baselines}

% Library baselines use PyTorch dispatch to cuBLAS (\texttt{torch.matmul} for GEMM),
% \texttt{nn.Conv2d} with default NCHW memory layout under standard cuDNN algorithm selection for convolution,
% \texttt{F.layer\_norm}/\texttt{F.rms\_norm} for normalization,
% and PyTorch eager execution for element-wise operators.
% Attention baselines use scaled dot-product attention with the FlashAttention backend
% (\texttt{enable\_flash\_sdp(True)}); results are presented separately because
% the Triton and TileLang implementations differ algorithmically from PyTorch's baseline.

For each workload, we compare DSL implementations against the corresponding vendor-library path exposed through PyTorch. GEMM baselines use cuBLAS via \texttt{torch.matmul}. Convolution baselines use PyTorch \texttt{nn.Conv2d} with default NCHW memory layout under standard cuDNN algorithm selection. Normalization baselines use \texttt{F.layer\_norm} and \texttt{F.rms\_norm}. Element-wise and reduction baselines use standard PyTorch eager execution.

For attention, we use PyTorch scaled dot-product attention with the FlashAttention backend enabled through \texttt{enable\_flash\_sdp(True)}. We report these results separately because the Triton and TileLang implementations are not algorithmically identical to the PyTorch baseline.


\subsection{Profiling Setup}
\label{sec:meth:profiling}

% \paragraph{End-to-end throughput.}
% For each (kernel, configuration) pair we measure wall-clock kernel time
% using CUDA events (\texttt{cudaEvent\allowbreak Record} / \texttt{cuda\allowbreak Event\allowbreak Elapsed\allowbreak Time}),
% averaged over 100 iterations.
% Throughput measurements warm up with 10 un-timed iterations before 100 timed iterations.
% We report GPU-side time only, excluding host-side dispatch overhead.
% All kernels are run in isolation (no concurrent workloads)
% to minimize measurement noise from GPU frequency scaling.

% \paragraph{Hardware performance counters.}
% For root-cause analysis (RQ2) we collect hardware counter profiles
% using NVIDIA Nsight Compute~\cite{nsightcompute2024}.
% We collect three counter groups via Nsight Compute: \emph{memory access} (global load/store efficiency, L1/L2 hit rates, coalescing, vectorized-load fraction), \emph{compute utilization} (SM occupancy, warp stall cycles, Tensor Core utilization), and \emph{instruction mix} (\texttt{cp.async} issue count, register spill).
% Each kernel is profiled in a single iteration; stability is verified within 2\% over five repeats.

We measure both end-to-end latency and hardware performance counters. Latency is used for all comparisons against library baselines, while hardware counters are used to investigate the causes of observed performance gaps.

\paragraph{End-to-end throughput.}
For each workload and input configuration, we measure host-synchronized GPU execution time using \texttt{time.perf\_counter()} bracketed by \texttt{torch.cuda.synchronize()} at entry and exit. Each measurement uses 10 warm-up iterations followed by 100 timed iterations, and we report the median over the timed runs. All results reflect GPU-side execution only and exclude host-side dispatch overhead. To reduce measurement noise, we run each workload in isolation without concurrent GPU activity. We pin the clocks for all timing at graphics 1215\,MHz and memory 1215\,MHz; the 1410\,MHz maximum power-caps under the 400\,W board limit, whereas 1215\,MHz holds flat, yielding a run-to-run relative standard deviation of 0.0--0.9\%.

\paragraph{Hardware performance counters.}
To support root-cause analysis, we collect hardware counter profiles with NVIDIA Nsight Compute~\cite{nsightcompute2024}. The RQ2 analysis is grounded in seven concrete counters: \texttt{gld\_efficiency} (global-load bytes-per-sector efficiency), \texttt{ldg\_per\_request} (sectors per load request), \texttt{n\_regs} (registers per thread), \texttt{n\_spills} (register spills), \texttt{warp\_stall\_long\_scoreboard} (memory-latency stall cycles), \texttt{warp\_stall\_barrier} (synchronization stall cycles), and \texttt{lts\_\_t\_sector\_hit\_rate} (L2 sector hit rate). Each profile is collected from a single execution, and we verify stability within 2\% across five repeated runs.

\paragraph{Correctness.}
Before any timing, we validate every DSL kernel against its library baseline using per-dtype numerical tolerances: \texttt{fp32} at \texttt{atol/rtol = 1e-5}, \texttt{fp16} at \texttt{1e-3}, and \texttt{bf16} at \texttt{1e-2}, with the tolerance relaxed by $2\times$ for reduction kernels to absorb order-of-summation differences. We additionally exercise an edge-case suite (NaN, Inf, denormal, and all-equal inputs) across all 22 kernels, observing 0 crashes. After applying the RQ3 mitigations, we revalidate the fixed kernels and obtain 5/5 passes, confirming the optimizations preserve numerical equivalence. We exclude FP32 GEMM from the correctness-gated comparison because its discrepancy is root-caused as silent TF32 lowering on the SM80 tensor cores rather than a kernel defect (see \cref{sec:analysis:jit}).


\subsection{Metrics}
\label{sec:meth:metrics}

% The primary metric is \textbf{library efficiency}
% $E_\text{lib} = t_\text{lib} / t_\text{DSL} \times 100\%$,
% where $t_\text{lib}$ is the cuBLAS/cuDNN baseline latency and $t_\text{DSL}$ is the DSL kernel latency
% on identical hardware and shape; 100\% indicates parity, below 100\% means the DSL is slower.
% Secondary metrics---throughput ($T = 2 \cdot \text{FLOPs}/t$), hardware efficiency
% ($E_\text{hw} = T/T_\text{peak}$), and memory bandwidth utilization
% ($B = \text{bytes}/({t \cdot B_\text{peak}})$)---are reported for root-cause analysis (RQ2).

Our primary metric is \textbf{library efficiency},
\[
E_{\mathrm{lib}} = \frac{t_{\mathrm{lib}}}{t_{\mathrm{DSL}}} \times 100\%,
\]
where $t_{\mathrm{lib}}$ denotes the latency of the cuBLAS or cuDNN baseline and $t_{\mathrm{DSL}}$ denotes the latency of the corresponding DSL implementation under the same hardware and input configuration. A value of 100\% indicates parity with the library baseline; lower values indicate that the DSL implementation is slower.

Secondary metrics: throughput ($T = 2 \cdot \text{FLOPs}/t$), hardware efficiency
($E_\text{hw} = T/T_\text{peak}$), and memory bandwidth utilization
($B = \text{bytes}/({t \cdot B_\text{peak}})$)---are used qualitatively in root-cause analysis (RQ2)
to interpret counter measurements; they are not tabulated separately.

\subsection{Evaluation-Gap and Heuristic Measurement}
\label{sec:meth:evalgap}

To test whether existing benchmarks admit slow kernels (RQ1), we run each naive DSL kernel through a KernelBench-style evaluator (\texttt{eval\_kernel\_against\_ref}), which overrides the candidate's input generators with the reference's, checks output equivalence on randomized inputs, and warns only when a kernel exceeds $10\times$ the reference speed; we record the pass/fail verdict and the measured slowdown against the PyTorch baseline.
To compute the roofline anchor (RQ3), we model each kernel's essential work---bytes moved for memory-bound kernels (input plus output tensors) or FLOPs for compute-bound kernels---and divide the achieved work-rate by the relevant hardware peak (HBM bandwidth or FP16 Tensor-Core throughput) for the measured GPU; the peak assumed for each kernel is recorded alongside its measurement.

\subsection{Experimental Setup}
\label{sec:meth:setup}

% \paragraph{Hardware.}
% All experiments run on an NVIDIA A100-SXM4-40GB (Ampere, \texttt{sm\_80}),
% with 108 SMs, 40~GB HBM2e memory (~1.5~TB/s peak bandwidth), a 40~MB L2 cache, and a 400~W power cap.
% NVIDIA Driver 610.43.02, CUDA Toolkit 12.8.

% \paragraph{Software.}
% PyTorch 2.8.0+cu128, Triton 3.4.0, TileLang 0.1.6.post1, bundled cuDNN,
% Nsight Compute 2026.2.0; all versions are pinned in a reproducibility manifest
% distributed with the benchmark suite.
% We run under default cuDNN flags
% and pre-warm the Triton auto-tuner before measurement.

\paragraph{Hardware.}
We measure on two primary datacenter architectures. The first is an NVIDIA A100-SXM4-40GB GPU (Ampere, \texttt{sm\_80}) with 108 SMs, 40\,GB HBM2e memory ($\sim$1.5\,TB/s peak memory bandwidth), a 40\,MB L2 cache, and a 400\,W power cap (NVIDIA Driver 610.43.02, CUDA 12.8), on which we measure the suite magnitude (\cref{sec:evaluation}) and root-cause counters (\cref{sec:analysis}). The second is an NVIDIA GH200 (Grace Hopper, \texttt{sm\_90}; 96\,GB HBM3 at $\sim$4.0\,TB/s, $\sim$989.5\,TFLOP/s FP16 Tensor-Core, clocks locked at 1320/2619\,MHz), on which we run the evaluation-gap demonstration (\cref{sec:eval:passesslow}) and validate the roofline heuristic (\cref{tab:roofline}). Per-architecture magnitudes are reported where they differ.


\paragraph{Software.}
Our software stack consists of PyTorch 2.8.0+cu128, Triton 3.4.0, TileLang 0.1.6.post1, bundled cuDNN, and Nsight Compute 2026.2.0. We pin all versions in a reproducibility manifest distributed with the benchmark suite. For all experiments, we run under default cuDNN flags and pre-warm the Triton auto-tuner before measurement.
