# ICSE Research Track #1787 artifact — pinned dependencies
# Pinned versions for all measurements (A100 / GH200 primary; A100-PCIE / H100 controls).
#
# Reproducible install (works on Ada / A100 / H100):
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu126
#
# Notes:
#   - The PyTorch cu126 wheel ships its own bundled CUDA runtime — the host only
#     needs a recent NVIDIA driver (>= 555) and matching toolkit for `ncu`.
#   - For Nsight Compute counters on sm_90 (H100), the system `ncu` must be
#     >= 2023.2 (CUDA Toolkit 12.x).
#   - Triton and TileLang JIT-recompile per target SM automatically; the same
#     pinned versions cover sm_80 / sm_89 / sm_90 without source edits.

torch==2.8.0+cu126
triton==3.4.0
tilelang==0.1.6.post1
